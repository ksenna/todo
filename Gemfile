source 'https://rubygems.org'

ruby '2.0.0'

gem 'sinatra'
# gem 'bootstrap-sass', '~> 3.1.1.0'

group :development do
	gem 'shotgun' # Launches local dev server
	gem 'tux'			# Launches irb-type environment that allows you to interact with code 
end

